@extends('layouts.app')
@section('content')
<div class="mb-4 grid gap-3 sm:flex sm:items-center sm:justify-between"><h1 class="text-xl font-semibold">{{ __('tenants.title') }}</h1>@can('create', App\Models\Tenant::class)<a class="tap-target flex items-center justify-center rounded bg-slate-900 px-4 text-sm font-medium text-white" href="{{ route('tenants.create') }}">{{ __('tenants.add') }}</a>@endcan</div>
<form class="mb-4 grid gap-3 rounded border bg-white p-3 sm:grid-cols-[1fr_12rem_auto]"><input name="search" value="{{ request('search') }}" placeholder="{{ __('tenants.search_placeholder') }}" class="tap-target w-full rounded border p-2"><select name="lifecycle" class="tap-target w-full rounded border p-2"><option value="active" @selected($lifecycle === 'active')>{{ __('tenants.lifecycle.active') }}</option><option value="archived" @selected($lifecycle === 'archived')>{{ __('tenants.lifecycle.archived') }}</option><option value="all" @selected($lifecycle === 'all')>{{ __('tenants.lifecycle.all') }}</option></select><button class="tap-target rounded bg-slate-900 px-4 text-white">{{ __('app.actions.search') }}</button></form>
<x-table min-width="min-w-[44rem]"><tbody>@foreach($tenants as $tenant)<tr class="border-t"><td class="p-4 font-medium">{{ $tenant->full_name }}</td><td class="p-4 whitespace-nowrap"><bdi dir="ltr">{{ $tenant->phone }}</bdi></td><td class="p-4 whitespace-nowrap">@if($tenant->archived_at)<span class="rounded bg-amber-100 px-2 py-1 text-xs font-medium text-amber-800">{{ __('tenants.lifecycle.archived') }}</span>@else<span class="rounded bg-emerald-100 px-2 py-1 text-xs font-medium text-emerald-800">{{ __('tenants.lifecycle.active') }}</span>@endif</td><td class="p-4 whitespace-nowrap"><a class="tap-target inline-flex items-center rounded border px-3 text-slate-700" href="{{ route('tenants.show', $tenant) }}">{{ __('app.actions.view') }}</a></td></tr>@endforeach</tbody></x-table>
{{ $tenants->links() }}
@endsection
