@extends('layouts.app')
@section('content')
<div class="mb-4 flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><h1 class="text-xl font-semibold">Contract {{ $contract->contract_number }}</h1><p class="mt-1 text-sm text-slate-600">This contract connects a tenant to a unit and defines the rent schedule.</p></div><div class="flex gap-2"><a class="rounded border px-3 py-2 text-sm" href="{{ route('contracts.pdf', $contract) }}">Download contract PDF</a><a class="rounded border px-3 py-2 text-sm" href="{{ route('contracts.edit', $contract) }}">Edit contract</a></div></div>
<div class="grid gap-3 rounded border bg-white p-4 sm:grid-cols-2"><p><span class="font-medium">Tenant:</span> {{ $contract->tenant->full_name }}</p><p><span class="font-medium">Unit:</span> {{ $contract->unit->unit_number }}</p><p><span class="font-medium">Contract number:</span> {{ $contract->contract_number }}</p><p><span class="font-medium">Start date:</span> {{ $contract->start_date->toDateString() }}</p><p><span class="font-medium">End date:</span> {{ $contract->end_date->toDateString() }}</p><p><span class="font-medium">Rent:</span> {{ number_format($contract->rent_amount, 2) }}</p><p><span class="font-medium">Deposit:</span> {{ number_format($contract->deposit_amount ?? 0, 2) }}</p><p><span class="font-medium">Frequency:</span> {{ $contract->payment_frequency }}</p><p><span class="font-medium">Status:</span> {{ $contract->status }}</p></div>
<h2 class="mb-2 mt-6 font-semibold">Payment schedule</h2>
<x-table><tbody>@foreach($contract->payments as $payment)<tr class="border-t"><td class="p-3">{{ $payment->due_date->toDateString() }}</td><td class="p-3">{{ number_format($payment->amount_due, 2) }}</td><td class="p-3">{{ $payment->status }}</td><td class="p-3"><a class="text-blue-700" href="{{ route('payments.edit', $payment) }}">Record</a></td></tr>@endforeach</tbody></x-table>
@endsection
